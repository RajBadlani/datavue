# npm resolves peer deps strictly and fails on conflicts (e.g. ollama-ai-provider
# wants zod@3 while the @ai-sdk/* packages use zod@4). pnpm is lenient about this
# locally; this makes npm — which Vercel/Render run on install — behave the same.
legacy-peer-deps=true
